apiVersion: v1
kind: Service
metadata:
  name: login
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  selector:
    app: login
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
